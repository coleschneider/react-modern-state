mutation createTask($input: CreateTaskInput!) {
  createTask(input: $input) {
    task {
      id
      title
      description
      tags
      color
      startDate
      dueDate
      remindMeAt
      completedAt
      index
    }
  }
}
